#!/bin/bash
# vim: ft=bash

set -x
set -euo pipefail

echo "* Install Destination is $DESTDIR"
echo "* Creating a build user"
useradd -m -G wheel abuild

pushd "$SRCDIR/yay"
echo "* Building yay"
sudo -u abuild -E -- makepkg --force
ls -la
install yay*.pkg.tar.zst "$DESTDIR/"
popd
