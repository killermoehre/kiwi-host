#!/bin/bash
# vim: ft=bash

set -x
set -euo pipefail

echo "* Install Destination is $DESTDIR"
echo "* Creating a build user"
useradd -m -G wheel abuild

declare -r -x PKGDEST="$DESTDIR"
pushd "$SRCDIR/yay"
sudo -u abuild -E -- makepkg
popd
