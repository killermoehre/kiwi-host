#!/bin/bash
# vim: ft=bash

set -x
set -euo pipefail

if [[ "$1" == 'build' ]]; then
    exit 0
fi


useradd -m -G wheel abuild

pacman --noconfirm -S /yay*
sudo -u abuild -E -- yay --noconfirm -S zypper dnf python-kiwi
